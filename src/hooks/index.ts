/**
 * Custom Hooks Index
 * 
 * Export all custom hooks from this file for cleaner imports.
 * Example: import { useScrollPosition, useWindowSize } from "@/hooks";
 */

// Re-export GSAP's useGSAP hook for convenience
export { useGSAP } from "@gsap/react";

// Export custom hooks here as they are created
// export { useScrollPosition } from "./useScrollPosition";
// export { useWindowSize } from "./useWindowSize";

